{
  "buildCommand": "npm run db:generate -w backend && npm run build -w admin-dashboard",
  "outputDirectory": "admin-dashboard/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
